{% import '_macros/global' as global %}
{{ global.image( block.image.one(), '-fit-cover' ) }}
{# <img src="{{ block.image.one().url }}" height="{{ block.image.one().height }}" width="{{ block.image.one().width }}" alt=""> #}


